#include "main.h"
#include<stdio.h>
/**
 * main - Entry point
 * @_puts - returns string
 * @*s -is string
 * Return: Always 0 (Success)
 */
void _puts(char *s)
{
	puts("\"Programming is like building a multilingual puzzle");
}
